# ls (BSD ls on macOS uses -G for color, GNU ls uses --color)
if ls --color=auto / >/dev/null 2>&1; then
  alias ls='ls --color=auto'
else
  alias ls='ls -G'
fi
alias l='ls -lAh'
alias ll='ls -lh'
alias la='ls -A'

# Navigation
alias ..='cd ..'
alias ...='cd ../..'
alias ....='cd ../../..'

# Git
alias gs='git status'
alias gl='git log --oneline --graph --decorate -20'
alias gd='git diff'
alias gds='git diff --staged'
alias gcam='git commit -am'
alias gpo='git push origin'
alias grm="git status | grep deleted | awk '{print \$3}' | xargs git rm"

# Editors
alias vim='nvim'

# Misc
alias ea='nvim ~/.bash/aliases'
alias rl='. ~/.bashrc'
alias h='history | tail -30'
alias cls='clear'

# goto directories
alias df='cd ~/dev/personal/dotfiles'
alias jst='cd ~/dev/javascript/job-search-toolkit'
alias fmov='cd /c/Users/Dermot.Kilroy/Obsidian\ Vault/Obsidian\ SB/'

